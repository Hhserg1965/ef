#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <time.h>
#include <sys/stat.h>
#include <sys/types.h>

#include <cassert>


#include <opencv2/core/core.hpp>
#include <opencv2/features2d/features2d.hpp>
//#include <opencv2/nonfree/features2d.hpp>

#include "opencv2/objdetect/objdetect.hpp"
#include "opencv2/imgproc/imgproc.hpp"
#include "opencv2/highgui/highgui.hpp"
#include <opencv2/ml/ml.hpp>

using namespace cv;

int main(int argc, char *argv[])
{
    fprintf(stderr,"DEL_FNC cvGBT_del_f DELETED \n"); fflush(stderr);

    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }
    for(int ii=0; ii < 1000; ++ii){
        Mat *ttt = new Mat(10000, 100*ii, CV_32S);
        delete ttt;
    }

    fprintf(stderr,"-- 5\n"); fflush(stderr);

     CvGBTrees * gbt = new CvGBTrees();
     delete gbt;
     fprintf(stderr,"++ 5\n"); fflush(stderr);


     for(int ii=0; ii < 1000; ++ii){
         Mat *ttt = new Mat(10000, 100*ii, CV_32S);
         delete ttt;
     }
     for(int ii=0; ii < 1000; ++ii){
         Mat *ttt = new Mat(10000, 100*ii, CV_32S);
         delete ttt;
     }
     for(int ii=0; ii < 1000; ++ii){
         Mat *ttt = new Mat(10000, 100*ii, CV_32S);
         delete ttt;
     }

     fprintf(stderr,"END OK -- 5!!!666\n"); fflush(stderr);


    return(0);
}
